#include <stdio.h>
#include <math.h>
#define TESTEAZA(i, j, n) (((i) > 0) && ((i) < (n)) && ((j) > 0) && ((j) < (n)) ? 1 : 0)
#define MEDIA(a, b, c) (((double)(a) + (b) + (c)) / 3)
#define POLINOM(x) ((int)(2*(pow((x),5)) + 3 * (pow((x), 4)) - (pow((x),3)) + 2 * (pow((x), 2)) - 8 * (x) + 5))
